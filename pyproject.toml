[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hostguard"
version = "0.1.0"
description = "Linux security baseline auditor"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["hostguard*"]